def Newton(x,n):
   p = 0.001
   r = x / n
   rn = x
   while p <= abs(r-rn):
       rn = x
       for i in range(1,n):
           rn = rn / r
       r = 0.5 * (rn + r)
   return r

n = int(input("Степень корня: "))
x = int(input("Число: "))
print(Newton(x,n))
